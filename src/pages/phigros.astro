---
import fs from "node:fs";
import path from "node:path";

import GalleryGrid from "../components/GalleryGrid";
import SiteNav from "../components/SiteNav.astro";
import "../styles/global.css";
import { SITE_NAME, absoluteUrl, assetOgImage } from "../lib/seo";
import { SITE_CSP } from "../lib/security";
import type { AssetItem } from "../lib/types";

const assets = readAssets("phigros-index.json");
const pageTitle = "Phigros 曲绘资源下载 - Unknnownnn";
const pageDescription = "高清 Phigros 曲绘、头像、曲包封面、April Fools 等图片资源，支持分类筛选与批量下载。";
const pageUrl = absoluteUrl("/phigros/");
const pageImage = assets[0] ? assetOgImage(assets[0]) : absoluteUrl("/site-icon.png");
const navLinks = [
  { href: "/", label: "首页" },
  { href: "/arcaea", label: "Arcaea" },
  { href: "/phigros", label: "Phigros" },
  { href: "/search", label: "搜索" },
];

function readAssets(filename: string): AssetItem[] {
  const filePath = path.join(process.cwd(), "public", "data", filename);
  if (!fs.existsSync(filePath)) {
    return [];
  }
  return JSON.parse(fs.readFileSync(filePath, "utf8")) as AssetItem[];
}
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/site-icon.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="canonical" href={pageUrl} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={pageDescription} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={SITE_NAME} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:url" content={pageUrl} />
    <meta property="og:image" content={pageImage} />
    <meta http-equiv="Content-Security-Policy" content={SITE_CSP} />
    <title>{pageTitle}</title>
  </head>
  <body class="theme-phigros">
    <SiteNav links={navLinks} />

    <main class="gallery-page-shell">
      <header class="gallery-page-hero glass-panel" data-reveal>
        <p class="eyebrow">Phigros</p>
        <h1>Phigros 图库</h1>
        <div class="gallery-hero-badges">
          <span>曲绘 · 头像 · 曲包封面 · April Fools</span>
          <span>按曲包筛选</span>
          <span>支持批量打包下载</span>
        </div>
      </header>

      <GalleryGrid assets={assets} game="Phigros" client:load />
    </main>
    <script is:inline src="/search-autocomplete.js"></script>
    <script type="module" src="/site-effects.js"></script>
  </body>
</html>
